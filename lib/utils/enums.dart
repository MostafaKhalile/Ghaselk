enum Photos {
  perosnal,
  id,
  form,
  driverLicncse,
  carFront,
  carBack,
}
enum Gender { male, female }
enum orderstype { waiting, workingon, done }
enum ordercategory { ghaseel, kai, ghaseelkai }
